@page
@model Sweets.practice.Pages.CandystoreS.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Candystore</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Candystore.CSID" />
            <div class="form-group">
                <label asp-for="Candystore.CSLocation" class="control-label"></label>
                <input asp-for="Candystore.CSLocation" class="form-control" />
                <span asp-validation-for="Candystore.CSLocation" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Candystore.CSName" class="control-label"></label>
                <input asp-for="Candystore.CSName" class="form-control" />
                <span asp-validation-for="Candystore.CSName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
